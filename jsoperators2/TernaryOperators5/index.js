// If a customer has more than 100 points,
// they are a 'gold' customer, otherwise,
// they are a 'silver' customer.

let pints = 110; // esli 90 to silver lox ept)((((((()))))))
let type = points > 100 ? 'gold' : 'silver';

console.log(type);